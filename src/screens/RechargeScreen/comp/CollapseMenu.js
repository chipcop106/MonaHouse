import React from 'react';import { View, Text, StyleSheet } from 'react-native';import { DrawerGroup, DrawerItem, Icon, Avatar } from '@ui-kitten/components';import PropTypes from 'prop-types';CollapseMenu.propTypes = {};const SmartphoneIcon = (props) => <Icon {...props} name="smartphone-outline" />;const BrowserIcon = (props) => <Icon {...props} name="browser-outline" />;const StarIcon = (props) => <Icon {...props} name="star" />;function CollapseMenu(props) {  const { data } = props;  const [selectedIndex, setSelectedIndex] = React.useState(null);  return (    <View      selectedIndex={selectedIndex}      onSelect={(index) => setSelectedIndex(index)}>      <DrawerGroup        title={(props) => (          <View {...props}>            <Text style={styles.txt}>Tài khoản ngân hàng</Text>          </View>        )}        accessoryLeft={SmartphoneIcon}>        <DrawerItem          title={(props) => (            <View {...props}>              <Text style={[styles.txt, { fontSize: 18, fontWeight: 'bold' }]}>                ACB              </Text>              <Text style={styles.txt}>CTY TNHH MONA MEDIA</Text>              <Text style={styles.txt}>Số tài khoản: 216341549</Text>              <Text style={styles.txt}>Chi nhánh: PGD Da Kao</Text>            </View>          )}          accessoryLeft={(props) => (            <Avatar              {...props}              resizeMode={'contain'}              source={require('~/../assets/acb-logo.png')}            />          )}        />      </DrawerGroup>      <DrawerGroup        title={(props) => (          <Text {...props}>            <Text style={styles.txt}>Ví điện tử</Text>          </Text>        )}        accessoryLeft={BrowserIcon}>        <DrawerItem          title={(props) => (            <View {...props}>              <Text style={[styles.txt,  { fontSize: 18, fontWeight: 'bold' } ]}>Momo:</Text>              <Text style={styles.txt}>0769220162</Text>            </View>          )}          accessoryLeft={StarIcon}        />      </DrawerGroup>    </View>  );}const styles = StyleSheet.create({  txt: {    fontSize: 16,  },});export default CollapseMenu;