import React from 'react';import { Input } from '@ui-kitten/components';import { currencyFormat } from '~/utils';const InputMoney = (props) => {  const [isFocus, setIsFocus] = React.useState(false);	const [value, setValue] = React.useState(()=> props?.value ?? 0);  return (    <Input      {...props}      keyboardType="numeric"      value={`${currencyFormat(value)}${!isFocus ? 'đ' : ''}`}      onChangeText={(nextValue) => {      	  const text = nextValue.replace(/[^0-9\-]/g, '');	        setValue(text);	        !!props.onChangeText ? props.onChangeText(text) : null;        }      }      onFocus={({ nativeEvent }) => setIsFocus(true)}      onBlur={({ nativeEvent }) => setIsFocus(false)}    />  );};export default InputMoney;